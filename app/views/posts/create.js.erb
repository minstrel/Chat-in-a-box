// $("#chat_area").prepend("Posted something new!<br>");
console.log("Posted something new");

// Pack name and message into message variable
var message = {
  postid: <%= @post.id %>
};

if ($("#post_picture").val()) {
  message.picturePresent = "true";
  message.pictureURL = "<%= @post.picture_url %>";
}

// Send the message
post_socket.trigger('in_message', message);

// Clear the text field
$("#post_text").val("");
$("#post_picture").val("");

